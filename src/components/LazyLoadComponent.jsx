
export const LazyLoadedComponent = () => {
    return (
        <div style={{
            padding: '20px', backgroundColor: '#333', borderRadius: '5px'
        }}>
            <p>
                Este Componente se ha cargado de manera dinamica utilazando React.Lazy
            </p>

        </div>
    )
}

export default LazyLoadedComponent;